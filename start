#!/bin/bash
set -euo pipefail

# When this image is launched by JupyterHub, run the Hub-aware single-user server
# so requests under /user/<name>/ are routed correctly.
if [[ -n "${JUPYTERHUB_API_TOKEN:-}" ]]; then
  exec jupyterhub-singleuser
fi

exec "$@"
